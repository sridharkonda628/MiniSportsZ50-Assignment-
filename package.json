{
    "name": "mini-sports-platform",
    "version": "1.0.0",
    "description": "Mini Sports Platform - MERN Stack",
    "scripts": {
        "start": "cd server && node server.js",
        "build": "npm install && cd client && npm install && npm run build && cd ../server && npm install && npx prisma generate"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "dependencies": {
        "concurrently": "^8.0.0"
    }
}